# Supabase — see SUPABASE.md for where to get these
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# College Scorecard (api.data.gov) — server-only, do NOT prefix with NEXT_PUBLIC_
COLLEGE_SCORECARD_API_KEY=

# Supabase service role — OPTIONAL, only needed to run ingestion scripts locally.
# Bypasses RLS, so keep it off any client/server component import path. Scripts only.
SUPABASE_SERVICE_ROLE_KEY=
